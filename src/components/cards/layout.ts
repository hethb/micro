import { space } from '@/theme/tokens';

/** Leaves room for the badges on top and the action rail on the right. */
export const CONTENT_PADDING = {
  paddingLeft: space.xl,
  paddingRight: 84,
  paddingTop: 96,
  paddingBottom: 48,
} as const;
